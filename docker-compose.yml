version: '3'
services:
  playground:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8888:8888'
    volumes:
      - experiments:/quantum-power-system-state-estimation

  test:
    build:
      context: .
      dockerfile: dev.dockerfile

volumes:
  experiments: